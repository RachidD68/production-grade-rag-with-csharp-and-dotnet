# Local infra credentials. Copy to `.env` and edit.
# `.env` is git-ignored — never commit real secrets.

# ---- Neo4j ----
NEO4J_USER=neo4j
NEO4J_PASSWORD=rag-in-dotnet

# ---- Optional: pin specific Ollama models (default: nomic-embed-text + llama3.2) ----
# OLLAMA_EMBED_MODEL=nomic-embed-text
# OLLAMA_CHAT_MODEL=llama3.2
