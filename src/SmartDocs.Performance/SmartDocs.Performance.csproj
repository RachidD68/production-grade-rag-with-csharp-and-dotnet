<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.AI.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Caching.Memory" />
    <!-- IDistributedCache (response/retrieval caches, Ch 21) + MemoryDistributedCache
         (the in-memory IDistributedCache used by the unit tests and the dev inner
         loop). The Redis-backed implementation comes from this same package; the
         version is pinned centrally at 10.0.9 — no new pin. -->
    <PackageReference Include="Microsoft.Extensions.Caching.StackExchangeRedis" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Http" />
    <PackageReference Include="Microsoft.Extensions.Http.Resilience" />
    <PackageReference Include="Polly" />
    <!-- OpenTelemetry tracing + metrics for the pipeline ActivitySource and the
         per-tenant cost counter (Ch 21). Versions pinned centrally. -->
    <PackageReference Include="OpenTelemetry" />
    <PackageReference Include="OpenTelemetry.Extensions.Hosting" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\SmartDocs.Core\SmartDocs.Core.csproj" />
    <ProjectReference Include="..\SmartDocs.Generation\SmartDocs.Generation.csproj" />
  </ItemGroup>

</Project>
