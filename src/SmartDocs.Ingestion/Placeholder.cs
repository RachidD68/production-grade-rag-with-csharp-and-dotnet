namespace SmartDocs.Ingestion;

/// <summary>
/// Placeholder for the <c>SmartDocs.Ingestion</c> project.
/// </summary>
/// <remarks>
/// <para>Populated in Phase 2: Ch 3–5, 7 — embeddings, chunking, multimodal, indexing.</para>
/// <para>This type exists only so the project compiles before any chapter code lands.
/// It is removed (or replaced) in the phase that introduces the first real type.</para>
/// </remarks>
internal static class Placeholder
{
    /// <summary>
    /// A no-op constant referenced from the project so the type is not stripped by linkers.
    /// </summary>
    public const string Project = "SmartDocs.Ingestion";
}
