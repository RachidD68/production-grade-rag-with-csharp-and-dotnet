<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Internal composition layer; not part of the published package set. -->
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    <!-- Orthogonal Procrustes solve in DriftAdapter (Ch 22): SVD of the
         cross-covariance matrix. Pinned centrally at 5.0.0. -->
    <PackageReference Include="MathNet.Numerics" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\SmartDocs.Core\SmartDocs.Core.csproj" />
    <!-- Ch 24 Compliance/* composes lower layers: the Ch 23 provenance signer +
         alert sink (Security), the Ch 24 audit-entry + citation shapes
         (Generation), and the faithfulness judge baseline (Evaluation). All
         three depend only on Core (Generation also on Retrieval), so Operations
         remains a leaf consumer — no dependency cycle. -->
    <ProjectReference Include="..\SmartDocs.Security\SmartDocs.Security.csproj" />
    <ProjectReference Include="..\SmartDocs.Generation\SmartDocs.Generation.csproj" />
    <ProjectReference Include="..\SmartDocs.Evaluation\SmartDocs.Evaluation.csproj" />
  </ItemGroup>

</Project>
